<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Requerido para consumir la API. Flutter solo lo inyecta en los manifests
         de debug/profile, así que sin esta línea un APK de release se queda sin red. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- El tracking en tiempo real necesita la ubicación del repartidor (geolocator). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!--
      Para que el reparto siga emitiendo con la app minimizada. Desde Android 10
      el sistema corta las actualizaciones de ubicación en segundo plano salvo
      que haya un servicio en primer plano; FOREGROUND_SERVICE_LOCATION es
      obligatorio desde Android 14 (targetSdk 34) o el servicio lanza
      SecurityException al arrancar.

      El servicio en sí no se declara aquí: geolocator_android ya aporta
      GeolocatorLocationService con foregroundServiceType="location".

      No se pide ACCESS_BACKGROUND_LOCATION: con el servicio iniciado mientras la
      app está visible basta, y así se evita la revisión extra de Google Play.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!--
      Sin esto, en Android 13+ la notificacion del servicio no se muestra: el
      reparto corre pero el repartidor no ve la confirmacion de que esta
      compartiendo su ubicacion.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="movil_logistica"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
